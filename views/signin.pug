extends layout

block main
  div
    h2(class="about") Fitpork stores and displays your personal record collection in a Pitchfork-inspired fashion.
    img(src="/images/logo.png" alt="flying pigs logo"  class="logo")
  
  
  form(action=`/users/signin` method="post")
    dl
      dt
        label(for="username") Username:
      dd
        input(type="text" name="username" placeholder="username" value=username)

      dt
        label(for="password") Password:
      dd
        input(type="password" name="password" placeholder="password")

    fieldset.actions
      input(type="submit" value="Sign In")
      
      input(type="submit" value="Register" formaction="/users/signup")
